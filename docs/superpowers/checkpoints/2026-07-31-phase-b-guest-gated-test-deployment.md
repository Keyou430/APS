# Phase B Guest-Gated Test Deployment

Date: 2026-07-31
Branch: `codex/hermes-platform-integration`
Verified application commit: `8887d7d`

## Local Verification

- Backend pytest: 193 passed.
- Frontend Vitest: 159 passed.
- Frontend build and lint: passed.
- Backend Ruff: passed.
- Phase B operations and guest invitation Playwright: 2 passed at 390/768/1024/1440.
- Gated live test-deployment Playwright: 1 passed; without live environment variables the spec is skipped.

## Test Server

- Existing test database backup created before deployment: `/opt/agent-platform-backups/phase-b-task11-preflight-20260731T092837Z.dump`, 98,957 bytes.
- Isolated database restored from the backup and verified at `20260729_0005`.
- Isolated Alembic roundtrip: `20260729_0005 -> 20260731_0007 -> 20260729_0005 -> 20260731_0007`.
- Isolated core counts remained `users=1, knowledge_entries=0, chat_sessions=3`; new invitation/citation tables existed and normalized email violations were zero.
- Test deployment services API, RAG worker, Web, database, Hermes, and Hermes knowledge were healthy after deployment.
- Test database is now at `20260731_0007 (head)`; no production database was touched.
- Synthetic API smoke: login 200, `/auth/me` returned organization context fields, operations 200 without raw content, invitations 404 while the guest flag is disabled, Web 200.
- A PostgreSQL-only invitation accept failure was found during synthetic acceptance: unrestricted `FOR UPDATE` included the nullable joined role table. Commit `e36bd11` scopes locks to `users` and `organization_memberships`; the PostgreSQL SQL contract and full regression pass.
- The guest flag was enabled only for a server-local synthetic account using the test adapter. Explicit resource access returned 200; organization visibility returned 404; agent, member, and operations endpoints returned 403; membership revoke returned 200 and the revoked token context returned 403.
- The synthetic user, invitation, grants, and resources were removed immediately after verification. The guest flag was returned to disabled and all six deployment services were healthy.
- The first real-browser upload exposed a frontend contract bug: the global JSON content type serialized `FormData`, so FastAPI returned 422 for missing `title` and `file`. Commit `18185e3` adds a failing multipart contract assertion and sends uploads as `multipart/form-data`; the Web service was rebuilt and remained healthy.
- A real browser then completed owner upload -> ready -> explicit share -> grantee knowledge AI -> structured citation -> owner revoke -> citation 404/unavailable state. The run monitored unexpected page errors and HTTP 5xx responses.
- Six knowledge entries created across the browser retries, both temporary internal users, their grants, sessions, and jobs were deleted. Residue checks returned zero temporary users, zero matching entries, and zero orphan ingestion jobs.
- Step 3 live authorization matrix passed on the test deployment with two temporary organizations, three internal memberships, and one guest. It covered list/detail/retrieve/download/chat/citation across private, organization-visible, active grant, expired grant, revoked grant, and cross-organization resources; active internal and guest grants were revoked and all post-revoke reads failed closed. Temporary users, organizations, resources, sessions, grants, jobs, and uploaded objects were removed by the acceptance script.
- Step 4 passed with two tabs in one in-app Browser session. Both tabs logged in as the same temporary two-organization admin; tab A switched to organization B while tab B remained in organization A. The API was recreated with a process-only one-minute access-token TTL, both tabs refreshed concurrently after expiry, and their UI organization state remained B/A. Database evidence showed one revoked predecessor and one newly active refresh token in each exercised organization family. The default 30-minute TTL was restored without changing `.env`; the temporary user and organizations were deleted and the API `/health` endpoint passed.
- Browser cleanup caveat: both directly closing the final in-app Browser tabs and ending an empty IAB session with `finalize({ keep: [] })` were followed by the Codex Desktop AppX container exiting. Until the lifecycle defect is fixed, never close the last tab or finalize an empty session; preserve at least one `handoff`/`deliverable` tab or leave the Browser session open.
- Step 5 preflight found that `VITE_FEATURE_EXTERNAL_GUESTS` was not forwarded through the production Web image build. A failing deployment contract test reproduced the gap; commit `2c5b20d` adds the Dockerfile/Compose build arg while preserving the false default. Full verification passed with backend 194, frontend 159, build, lint, and Ruff.
- The first Step 5 Browser attempt stopped before page initialization because this task's browser-control kernel again failed to materialize its temporary assets even after a task-local reset. The temporary owner, organization, resources, and jobs were removed; API and Web guest flags were restored to false, RAG worker was restarted, and deployment services returned healthy.
- Step 5 Browser acceptance then passed in the in-app Browser. Operations retry changed a failed job to queued and cancel changed a queued job to cancelled. The owner created a one-time test-adapter invitation for an explicit resource; a second tab accepted it, created the guest account, and logged in. Guest access showed only the granted resource, expired membership returned 403 and zero entries, and after the frontend fix in `b519e62` the guest page hid upload and source-selection controls and redirected `/assistant` back to `/knowledge`. The temporary org/users/grants/invitation/jobs were deleted, and the deployment was rebuilt with `FEATURE_EXTERNAL_GUESTS=false`; API health and all active services passed.
- A post-deployment portal/workspace report exposed missing real-API routes: the Web requested `/api/enterprise`, `/api/enterprise/portal`, `/api/dashboard`, and `/api/dashboard/layout`, but the backend returned 404. Commits `3ff567a` and `d727611` add internal-member-only, current-organization-scoped responses and omit nullable optional layout fields. TDD reproduced the 404 first; full verification passed with backend 196, frontend 160, build, lint, and Ruff. After API-only test deployment, all four routes returned 200 through the Web proxy, all services were healthy, and `FEATURE_EXTERNAL_GUESTS=false` remained unchanged. The in-app Browser could not be rerun because its task-local kernel asset path failed before page initialization even after reset; no standalone Playwright fallback was used.
- A user DOCX and PDF upload exposed two separate ingestion defects. The frontend's fixed 18-second polling window reported any non-terminal job as failed and only inserted the source after ready; `3b075b6` inserts queued/processing sources immediately, replaces timeout failure with a continuing parsing state, and updates by source ID. The PDF worker failure traced to missing `libxcb.so.1` and related Docling native libraries; `3b075b6` adds them and `37e3acb` preserves the cached Python dependency layer. Manual retry at the three-attempt limit was also unclaimable because attempts remained exhausted; `d8475d8` resets the manual retry budget. Full verification passed with backend 197, frontend 162, build, lint, and Ruff. On the test deployment both original entries ended ready with one database entry each; the retried 28-page PDF completed in one attempt with 37 chunks. Browser verification confirmed the workspace no longer shows role-recommendation task/calendar sections, opens the dashboard on Task Load, uses Smart Tasks instead of AI Pipeline, and reports no console warnings/errors. The temporary Browser user and tokens were removed, the workbench tab was preserved as `deliverable`, and all services were healthy with `FEATURE_EXTERNAL_GUESTS=false` unchanged.
- Commit `8887d7d` replaced the approved 503 placeholder with a complete SMTP delivery adapter while retaining the test adapter, URL-fragment acceptance, recipient allowlist, and fail-closed delivery rollback. TDD and serial full verification passed: backend 237, frontend 204, Ruff, lint, and build. The first parallel full run was discarded because concurrent Vitest imports and shared SQLite teardown contended for local resources; no timeout was relaxed.
- Before the bounded live trial, the shared test database was backed up to `/opt/agent-platform-backups/pre-phase-c-smtp-8887d7d-20260804-104605.dump` (579,607 bytes, mode 600, SHA-256 `859d6337bab282eb5cdb8737d099e518dc562f6d8bd6e429334c1f4dfe24bbb1`) and its `pg_restore` catalog was validated. The test database was migrated from `20260731_0007` to `20260803_0011`; required tables, `knowledge_entries.collection_id`, and same-organization collection/grant invariants passed. No production database was touched.
- With explicit authorization, the guest flag and SMTP configuration were enabled only for one allowlisted test recipient. The API returned no token, stored only a 64-character digest, and wrote `invitation.create` plus `invitation.deliver`; the test mailbox received the message. The in-app Browser accepted the fragment link, created and logged in the temporary guest, and showed only the explicitly granted resource. Guest navigation exposed only Knowledge; agent creation returned 403; the resource access tab allowed only the guest's own read permission and exposed no re-share action.
- Live knowledge-chat verification covered SSE, history, stop, delete, and the tool-less boundary. Completed streams contained knowledge context/text/terminal events; an active run stop returned 200 and persisted `cancelled`; history returned user/assistant records and session deletion returned 204. No tool/file/terminal/approval events, provider/object-storage key markers, or sensitive audit fields were present. Mobile width 390 had no horizontal overflow. The temporary guest, membership, refresh tokens, sessions, invitation, grant, and knowledge entry were deleted; residue counts were zero.
- The deployment was rebuilt from the base environment only. `FEATURE_EXTERNAL_GUESTS=false`, delivery adapter `test`, empty SMTP secret/allowlist, disabled acceptance returning 404, zero API 5xx responses, and six healthy services were verified. The temporary SMTP override file was removed; the clean release remained at `8887d7d`; the database remained at `20260803_0011 (head)`. A login page was preserved as the in-app Browser deliverable tab.
- Known residual: Arco Design 2.x `Message.error` calls the removed `ReactDOM.render` API under React 19 when expected 404 responses are surfaced. The gated test precisely excludes only `lt.render is not a function`; all other page errors and HTTP 5xx responses still fail the run. This dependency compatibility issue does not bypass the functional authorization assertions.

## Gate

`FEATURE_EXTERNAL_GUESTS` remains disabled after the explicitly authorized, allowlisted SMTP trial. Phase B Task 11 Steps 6 and 7 are complete. The remaining gates are unchanged: no anonymous sharing, general external-guest rollout, production migration, or production SMTP configuration is authorized; each requires its own deployment, rollback, privacy, and support approval.
